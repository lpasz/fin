
name: Fly Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Deploy app
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: superfly/fly-elixir-action@1.2
        with:
          fly_api_token: ${{ secrets.FLY_API_TOKEN }}
